# 打包问题总结

## 1. 问题：ASAR 模块加载失败

`Error: Cannot find module 'call-bind-apply-helpers'`

**原因**：electron-builder 打包时重建 node_modules 的目录结构，npm 扁平 hoist 的包被移动到嵌套位置，导致其他包 require 时找不到。

**解决**：关闭 ASAR 压缩，改用 `extraResources` 直接完整复制 `node_modules`，保持原始目录结构不变。

```json
"asar": false,
"extraResources": [
  { "from": "node_modules", "to": "app/node_modules" }
]
```

---

## 2. 问题：前端硬编码 localhost:5000

上传文件时后端没连上，请求全部失败

**原因**：前端所有 axios 请求都写死了 `http://localhost:5000/api/...`，但生产模式下 Express 监听的是随机端口（port 0），不是 5000。

**解决**：全部改为相对路径 `/api/...`，自动解析到当前页面所在端口。

---

## 3. 问题：Express v5 通配符路由不兼容

`TypeError: Missing parameter name at index 1: *`

**原因**：Express v5 使用 `path-to-regexp` v8，不再支持 `*` 作为通配符路由模式。`app.get("*", handler)` 报错。

**解决**：改为 `app.use(handler)` 中间件形式，避免路径匹配。

---

## 4. 问题：marked ESM 模块无法 require

`Error [ERR_REQUIRE_ESM]: require() of ES Module`

**原因**：`marked` v17 是 ESM-only 包，但 `upload.js` 用 CommonJS 的 `require()` 加载。Node.js v22 支持 require(esm)，但 Electron 内置的 Node.js 不支持。

**解决**：移除 `marked` 依赖，改用内置 `mdToHtml()` 简易转换器处理 markdown→HTML。

---

## 5. 问题：错误信息不可见

双击 exe 没有反应，也没有报错弹窗

**原因**：`console.error` 输出的错误在 GUI 应用中不可见，且 `app.quit()` 直接退出不留下任何痕迹。

**解决**：添加全局 `process.on("uncaughtException")` 和 `dialog.showErrorBox()` 弹窗显示错误。

---

## 6. 问题：打包后 upload.js 缺少 db 导入

上传文件时全部显示"导入失败"

**原因**：替换 marked 代码时误删了 `const db = require("../models/database")` 这一行，导致 `db.insertAsync()` 引用未定义变量。

**解决**：补回 `db` 导入。

---

## 7. 问题：files 配置过滤了 node_modules

打包后 app 目录只有少量包，大量依赖丢失

**原因**：自定义 `"files": ["electron/**/*", "server/**/*", ...]` 没有包含 `node_modules`，electron-builder 只打包了显式声明的文件。

**解决**：移除 `files` 过滤（用 `extraResources` 替代），或显式加上 `"node_modules/**/*"`。

---

## 最终打包配置 (package.json build 字段)

```json
{
  "files": ["electron/**/*", "server/**/*", "client/dist/**/*", "package.json"],
  "asar": false,
  "extraResources": [
    {
      "from": "node_modules",
      "to": "app/node_modules",
      "filter": [
        "**/*",
        "!electron/**",
        "!7zip-bin/**",
        "!app-builder-bin/**",
        "!builder-util/**",
        "!@electron/**",
        "!@malept/**",
        "!@types/**"
      ]
    }
  ]
}
```

## 最终安装包体积

- 绿色版目录：~56 MB
- NSIS 安装包：~90 MB

## 待解决问题

其他系统的还要再等一会...... 1.打包后 启动界面好丑 样式美化2.机器人回答问题太慢?
